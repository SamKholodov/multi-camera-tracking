# PyTorch (CUDA) ставится отдельно — см. scripts\install_torch_cuda.ps1 или комментарий внизу.
# После torch:  pip install -r requirements.txt

ultralytics>=8.3.0,<9
# boxmot — только для BotSort / core.oc_sort.ocsort; конфликтует с новым torchvision, ставь вручную при необходимости.
numpy>=1.26.0,<3
scipy>=1.11.0,<2
opencv-python>=4.8.0,<5
PyYAML>=6.0.1,<7
filterpy>=1.4.5,<2
lapx>=0.5.5
motmetrics>=1.4.0,<2
matplotlib>=3.8.0,<4
